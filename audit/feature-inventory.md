# PFOS Feature Inventory — Initial Scan

This initial inventory is autogenerated from a quick repo scan (navigation, app routes, and feature-related files). Columns marked *TBD* require per-file verification in Phase 1.

| Feature | Route | UI Status | Backend Status | Firebase Status | CRUD Status | Mobile Status | Integration Status | Completion |
|---|---:|---|---|---|---|---|---|---:|
| Dashboard | /dashboard | Present | Uses `dashboard.service` (needs verification) | TBD | TBD | TBD | Integrates widgets, transactions | Partial |
| Transactions | /dashboard/transactions, /transactions/add | Present (pages + `useTransactions`) | `useTransactions` exists and `transactions.service` implements Firestore CRUD | `transactions` collection referenced | Create/Read/Update/Delete implemented via `transactions.service` and `TransactionContext` (offline queue supported) | Mobile flows exist in components | Import Hub integration | Complete |
| Accounts | /dashboard/accounts | Present (`AccountsPage`, `AccountCard`, `AddAccountModal`) | `useAccounts` hook and `AccountContext` implement CRUD with `accounts.service` | `accounts` collection referenced | Create/Read/Update/Delete implemented (soft delete supported); optimistic updates and offline queue present | Mobile account flows present | Linked to net worth & balances (Complete) | Complete |
| Notifications | /dashboard/notifications | UI (widget, hooks) present | `notifications.service` used in hooks | `notifications` collection referenced | Create/Read referenced in hooks | TBD | Linked to settlements & hooks | Partial |
| Audit Center | /dashboard/audit | Present (`app/dashboard/audit/page.tsx`) | `audit.service` present | `auditLogs`, `errorHistory` referenced | Read/Write/seed (seed gated) | TBD | Export report placeholders | Partial |
| Goals | /dashboard/goals | Present (nav) | `useGoals`/service referenced? *TBD* | TBD | TBD | TBD | TBD | Missing/Partial |
| Investments | /dashboard/investments | Present (nav & components) | `useInvestments` referenced | TBD | TBD | TBD | Portfolio widgets present | Partial |
| Budgets | /dashboard/budgets | Present (`budget-card`) | Budget hooks/services referenced | `budgets` collection? *TBD* | TBD | TBD | Alerts integration | Partial |
| Analytics & Reports | /dashboard/analytics, /dashboard/reports | Present (cards & charts) | Analytics hooks use transactions | TBD | Read-only charts present; exports TBD | TBD | Exports & AI Coach referenced | Partial |
| AI Coach | /dashboard/ai-coach | Nav entry present | AI features referenced (needs verification) | Uses user data? *TBD* | N/A | TBD | External AI integration *TBD* | Missing/Partial |
| Import Hub | /dashboard/import | Present (import utils exist) | Bank statement parsers present (`BankStatementMapper`) | Writes transactions via import flows | Create via import exists | Mobile import TBD | CSV/XLSX libs included | Partial |
| Subscriptions & Recurring | /dashboard/subscriptions, /dashboard/recurring | Present (hooks `useSubscriptions`) | Hooks reference transactions | TBD | Notification hooks for recurring payments | TBD | Optimization suggestions present | Partial |
| Documents / Vault | /dashboard/documents | Present (nav) | Document upload/storage services? *TBD* | Likely uses storage | Upload/Download TBD | Mobile: document viewer TBD | Partial |
| Settings & Security | /dashboard/settings, /dashboard/security | Present | Settings services referenced | TBD | CRUD on user prefs TBD | TBD | Partial |
| Trading Journal | /dashboard/trading-journal | Nav entry present | `useTradingJournal` hook exists | TBD | Create/Read present | TBD | Partial |
| EMI / Settlements / Family / Policies / Wealth Planner / Tax Center / Retirement | multiple routes in nav | Nav entries present; pages/components may exist | Services/hooks referenced in many places | TBD | TBD | TBD | Partial/Missing |

Notes:
- `lib/navigation.ts` was used as the primary list of routes and features to seed this inventory.
- Several components reference hooks in `src/hooks` and services in `src/services/firestore` — these need per-feature verification for CRUD and Firebase rules/index checks.
- Some simulator/seed controls exist (e.g., `audit.seedAuditLogs`) — these are gated or disabled for production in prior changes.

## Notifications Audit — Live Test Update

- Status: In progress (real verification attempted).
- Actions performed: executed a live Firestore client script to create a notification for user `audit-integration-user`, then attempted to read latest notifications and unread count.
- Result: Firestore rejected the client write with `permission-denied` (missing/insufficient permissions). This indicates security rules require an authenticated client or server-side/admin credentials to write user notifications.
- What passed: code inspection — `src/services/firestore/notifications.service.ts` implements
	- createNotification, getUserNotifications, subscribeToUserNotifications (realtime), getUnreadCount,
	- updateNotification, deleteNotification, markAsRead, markAllAsRead, pin/dismiss/restore, cleanupExpiredNotifications.
- What still needs action:
	1. Run live verification in an authenticated browser session (recommended: run app locally, sign in as a test user, exercise event flows).
	2. OR use Firebase Admin credentials (service account) to run server-side integration scripts to validate create/read/update/delete and cleanup flows.
	3. Validate SmartAlertEngine triggers by simulating domain events (transactions/budgets/goals/etc.) while authenticated.

Next steps (recommended):
- Start the app locally (`npm run dev`), sign in with a test account, then exercise the flows listed under Phase 7 to confirm realtime propagation to UI (badge, RealtimeNotificationStack, NotificationsPage, unread counts).
- If CI/server-side tests are required, provide Firebase Admin service account credentials or set up emulator with permissive rules for the audit run.

Next steps:
1. Phase 1 detailed per-file verification: open each route/component and record exact statuses for UI, backend, Firebase, CRUD, mobile, and integrations.
2. Start Phase 2 module validation with `Accounts` and `Transactions` modules (high-priority flows).
3. Run `npm run lint` and `npx tsc --noEmit` after each change batch.

Generated by Copilot assistant as initial artifact — further updates will be committed to this file during the audit.
